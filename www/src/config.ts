export type Config = {
    width: number,
    height: number,
    pixelSize: number,
    frameDurationMs: number,
}

export const config = {
    width: 8,
    height: 8,
    pixelSize: 30,
    frameDurationMs: 300,
}
